{
  "name": "fix-math",
  "version": "1.0.9",
  "scripts": {
    "build": "esbuild main.ts --bundle --outfile=main.js --format=cjs --platform=node --external:obsidian"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "obsidian": "latest"
  }
}